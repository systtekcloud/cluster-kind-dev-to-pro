apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-app00
  namespace: frontend-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: default-app
  template:
    metadata:
      labels:
        app: default-app
    spec:
      containers:
      - name: nginx-app00
        image: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-app00-config
          mountPath: /usr/share/nginx/html
      volumes:
      - name: nginx-app00-config
        configMap:
          name: nginx-app00-config
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-app00-service
  namespace: frontend-ns
spec:
  selector:
    app: default-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-app00-config
  namespace: frontend-ns
data:
  index.html: |
    <html>
      <head><title>Bienvenido</title></head>
      <body style="text-align: center;">
        <h1>🚀 Bienvenido a mi Microservicio por Defecto 🚀</h1>
        <p>Esta es la página por defecto para myapps.local</p>
      </body>
    </html>